name: QUAM pytest

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: "pip"
        cache-dependency-path: pyproject.toml

    - name: Install dev dependencies
      run: |
        python -m pip install --upgrade pip
        pip install .[dev]

    - name: Black formatting check
      run: python -m black --check quam/

    - name: Flake8 linting
      run: python -m pflake8 quam/config/

    - name: Mypy type check
      run: python -m mypy quam/ --ignore-missing-imports --follow-imports=silent

  run-tests:
    uses: ./.github/workflows/run_tests.yaml
    secrets: inherit
